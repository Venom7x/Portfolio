// Create a free account at https://www.emailjs.com, then replace these
// three values with your Service ID, Template ID and Public Key.
// Your email template should include {{name}}, {{email}}, {{subject}}
// and {{message}} variables to match the fields collected below.
export const emailjsConfig = {
  serviceId: "YOUR_EMAILJS_SERVICE_ID",
  templateId: "YOUR_EMAILJS_TEMPLATE_ID",
  publicKey: "YOUR_EMAILJS_PUBLIC_KEY",
};
